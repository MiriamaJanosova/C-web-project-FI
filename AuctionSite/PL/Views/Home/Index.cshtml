@model PL.Models.Home.IndexModel

   
@using (Html.BeginForm("Search", "Auctions", FormMethod.Post))
{
    <div class="row">
        <div class="col-lg-6">
            <div class="input-group">
                <span class="input-group-btn">
                    <button class="btn btn-default" type="submit" value="Submit">Search</button>
                </span>
                <input id="searchedText" type="text" name="searchedText" class="form-control" placeholder="Search for..." />
            </div>
        </div>
    </div>
}